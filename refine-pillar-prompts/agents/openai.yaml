interface:
  display_name: "Refine Pillar Prompts"
  short_description: "Evidence-led SPS prompt refinement"
  default_prompt: "Use $refine-pillar-prompts to produce a numbered, evidence-backed pillar prompt-learning brief and apply only the item indexes I select."

dependencies:
  tools:
    - type: "mcp"
      value: "social_page_studio"
      description: "Preferred for reading evidence and versioned prompts, and applying only user-selected prompt recommendations."
