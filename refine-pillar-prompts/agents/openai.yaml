interface:
  display_name: "Refine Pillar Prompts"
  short_description: "Performance-led SPS learnings and editorial reports"
  default_prompt: "Use $refine-pillar-prompts to run a performance-first evidence review and produce either a stable editorial report or a numbered prompt-learning brief, applying SPS changes only when I explicitly select item indexes."

dependencies:
  tools:
    - type: "mcp"
      value: "social_page_studio"
      description: "Preferred for reading evidence and versioned prompts, and applying only user-selected prompt recommendations."
