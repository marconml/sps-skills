interface:
  display_name: "怪獸字修復 · Fix Monster Glyphs"
  short_description: "Compare fonts, stretch to measured bounds, match style"
  default_prompt: "Use $fix-monster-glyphs with exactly three steps: find a matching font, stretch and overlay the correct glyph to measured original bounds, then match the original styling."
